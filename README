
# **A FAIRE :

Amaury --> BUILTIN
Arthur -->
- enregistrer token vide "" --> OK
- chiffre n'arrete pas lexpansion --> OK
- si '/' juste derriere variable alors literal --> sujet precise quil ne faut pas le gerer --> OK

-ajt une option pour detecter les quotes au sein d'une variable -> ajouter liste chainee de segment au token puis ajout de l'option a expander
Gestion des signaux + si '/' juste derriere variable alors literal + chiffre n'arrete pas lexpansion

GESTION DES SIGNAUX :
- s'occuper que du mode interactif (en attente de commande comme le demande le sujet)
CTRL + C --> sur prompt en att de cmd, nouveau prompt / pendant cmd, tue la commande (brutalement renvoie SIGINT)
CTRL + D --> sur prompt en att de cmd, quitte le shell / pendant cmd, renvoie EOF ce qui a pour effet de stopper certaine cmd en cours (exemple heredoc) mais ne fait rien sur un sleep 10 par ex
et cela pcq ctrl+D ferme stdin
