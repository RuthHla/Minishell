utiliser : valgrind --leak-check=full --show-leak-kinds=all --suppressions=readline.supp ./minishell

plus de leak/probleme de fd reste ouvert mais plus 100% fonctionnel  cause du code erreur principalement